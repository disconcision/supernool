Edit this concept sheet. Preserve the title and rows 2, 3, 4, and 5 exactly. Replace ONLY the drawings within row 1 RETRACT / EMERGE, keeping the same paper, pen style and row bounds. The existing row incorrectly deletes all branches and reverses leaf order. Correct it to four precise sequential rooted tree diagrams with THREE permanent leaf tips a,b,c in fixed left-to-right order in EVERY frame. Use simple thin curved woody lines with explicit small circular joints if necessary for correctness. Frame1 root at bottom branches to internal joint at left and leaf c on right; the left internal joint branches to leaves a and b. Frame2 the same left joint moves halfway down toward root, a,b,c remain. Frame3 the internal joint coincides with root: one root with exactly three branches to a,b,c. Frame4 root branches to leaf a at left and internal joint at right; that right joint branches to leaves b,c. Keep leaf colors stable: a sage, b gray-green, c ochre. No leaf vanishes, no stump-only frame, no permutation, no extra leaves. The four frames must clearly show ((a,b),c) -> contracted three-way joint -> (a,(b,c)), through the intermediate halfway contraction. Keep the rest of the image unchanged.
