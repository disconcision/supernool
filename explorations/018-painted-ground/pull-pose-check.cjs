const {chromium}=require('playwright');const assert=require('node:assert/strict');
(async()=>{const browser=await chromium.launch({channel:'chrome',headless:true});try{const page=await browser.newPage({viewport:{width:1280,height:720},deviceScaleFactor:1});const errors=[];page.on('pageerror',e=>errors.push(e.message));await page.goto('http://127.0.0.1:3100/explorations/018-painted-ground/?mode=body');await page.waitForFunction(()=>document.querySelector('#world').dataset.meshing==='ready');
await page.locator('#hudToggle').click();await page.screenshot({path:__dirname+'/assets/hands-front.png'});await page.keyboard.press('Escape');
await page.getByRole('button',{name:'Open Help',exact:true}).click();await page.locator('#approach').click();await page.waitForFunction(()=>document.querySelector('#world').dataset.near==='true');await page.waitForTimeout(2600);await page.locator('#world canvas').focus();await page.keyboard.press('Space');await page.keyboard.press('h');await page.waitForTimeout(150);await page.keyboard.down('Space');await page.keyboard.press('1');await page.keyboard.down('ArrowDown');await page.waitForTimeout(800);await page.keyboard.up('ArrowDown');
const state=await page.locator('#world').evaluate(e=>({...e.dataset}));assert.equal(state.controlPhase,'pull');assert.equal(state.activeHand,'right');const [x,z]=state.player.split(',').map(Number),target=Math.atan2(-1-x,-3-z),difference=Math.atan2(Math.sin(+state.facing-target),Math.cos(+state.facing-target));assert(Math.abs(difference)<.015,'Backpedaling must face the tree');
await page.locator('#hudToggle').click();await page.screenshot({path:__dirname+'/assets/hands-backpedal.png'});await page.keyboard.press('Escape');await page.keyboard.press('Escape');await page.keyboard.up('Space');await page.waitForFunction(()=>document.querySelector('#world').dataset.busy==='false');assert.deepEqual(errors,[]);console.log('Actual body pull faces the tree while backpedaling, uses the right hand, and cancels cleanly.');}finally{await browser.close();}})().catch(e=>{console.error(e);process.exit(1)});
